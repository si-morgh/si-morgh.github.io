# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler (.bundle/config is tracked on purpose; installed gems are not)
vendor/
Gemfile.lock

# macOS
.DS_Store

# Editors
.idea/
*.swp

# Untouched image masters, kept locally and never published
_originals/
