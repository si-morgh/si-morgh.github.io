---
layout: default
title: Collection One
section: art
images:
  - file: 1.png
    title: "First Painting"
  - file: 2.png
    title: "Second Painting"
---

<div class="carousel-overlay" id="galleryOverlay">
  <button class="close-gallery" id="closeBtn">&times;</button>
  <div class="carousel-container">
    <div class="carousel-slide" id="carouselSlide">
      {% for img in page.images %}
        <div class="art-item">
          <img src="{{ '/assets/images/art/collections/C1/' | append: img.file | relative_url }}" alt="{{ img.title }}">
          <div class="art-caption">{{ img.title }}</div>
        </div>
      {% endfor %}
    </div>
    <div id="image-counter">1 / {{ page.images.size }}</div>
    <button class="carousel-prev" id="prevBtn">❮</button>
    <button class="carousel-next" id="nextBtn">❯</button>
  </div>
</div>

<script>
  /* Paste the same JS we used before here */
  /* CHANGE the Close Button logic to this: */
  document.getElementById('closeBtn').addEventListener('click', () => {
    window.location.href = "{{ '/Art/collections/' | relative_url }}"; 
  });
</script>